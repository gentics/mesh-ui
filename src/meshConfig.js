(function(window, document) {

    /**
     *
     * @name meshConfig
     */
    var meshConfig = {
        // The URL to the Mesh API
        apiUrl: window.location.protocol + '//' + window.location.hostname + '/gentics/mesh-mock-backend'
    };


    window.meshConfig = meshConfig;

})(window, document);